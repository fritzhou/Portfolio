package com.fritzvohn.airnudge.gesture;

/** Gestures emitted by the hand-tracking pipeline. */
public enum AirGesture {
    SWIPE_UP,
    SWIPE_DOWN,
    SWIPE_LEFT,
    SWIPE_RIGHT,
    CLOSED_FIST,
    PINCH
}

package com.fritzvohn.airnudge.settings;

/** Android actions that a recognized gesture can trigger. */
public enum UserAction {
    NONE,
    SWIPE_UP,
    SWIPE_DOWN,
    SWIPE_LEFT,
    SWIPE_RIGHT,
    BACK
}
